/// Solve the problem for day one, given the provided data.
pub fn solve(data: &Vec<String>) -> Vec<u32> {
    vec![]
}

#[cfg(test)]
mod tests {
    use crate::day1::solve;

    #[test]
    fn test_day_1() {
        // Wip
        let result = solve(
            &crate::utils::read_input_for_day(&1).expect("Expect the data file to be there."),
        );
        assert!(false);
    }
}
